#include "bits/stdc++.h"
using namespace std;
using ll = long long;

vector<vector<int>> mult_matriz(vector<vector<int>> A, vector<vector<int>> B){
    vector<vector<int>> C;
    C.resize(A.size());
}

int main(){
    int n;
    while (cin >> n){
        if(n == 0) break;
        vector<int> inp;
        for(int i=0;i<4;i++){
            int k;
            cin >> k;
            inp.push_back(k);
        }
        vector<vector<int>> A;
        vector<vector<int>> base;
        vector<vector<int>> U;

        A.resize(3);
        U.resize(3);
        base.resize(3);
    }
}